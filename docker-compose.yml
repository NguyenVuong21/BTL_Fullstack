version: '3.8'

services:
  nexus-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: Nexus_SQL_Server
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=PasswordYour123!